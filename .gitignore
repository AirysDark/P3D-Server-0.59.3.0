# --- Everything in root should be tracked ---
# Disable all Visual Studio ignores

# Force include everything (override any default rules)
!*/

# Explicitly allow all files in the repo
!.*

# No ignores — push everything from root